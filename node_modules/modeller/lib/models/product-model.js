'use strict';

const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const ProductSchema = new Schema({
  name: { type: String },
  weight: { type: String },
  smells: [{ type: Schema.Types.ObjectId, ref: 'SmellModel' }],
  gifts: [{ type: Schema.Types.ObjectId, ref: 'GiftModel' }],
  price: { type: Number },
  productType: { type: Schema.Types.ObjectId, ref: 'ProductTypeModel' },
  quantity: { type: Number },
  // filter 
  status: { type: Boolean, default: true },
  slug: { type: String },
  activated: { type: Boolean, default: true },
  deleted: { type: Boolean, default: false },
  createdAt: { type: Date },
  createdBy: { type: String },
  updatedAt: { type: Date },
  updatedBy: { type: String },
});

const Product = mongoose.model('ProductModel', ProductSchema, 'products');
module.exports = Product;